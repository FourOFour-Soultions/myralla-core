spring.application.name=loyalty

spring.datasource.url=jdbc:postgresql://localhost:5432/myralla
spring.datasource.username=myralla
spring.datasource.password=myralla
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=123
spring.redis.database=0

# Enable specific Actuator endpoints
management.endpoints.web.exposure.include=health,info,metrics

# Secure Actuator endpoints (optional)
management.endpoints.web.exposure.exclude=env
management.endpoints.web.base-path=/actuator